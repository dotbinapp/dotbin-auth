{
  "name": "dotbin-auth",
  "version": "0.1.0",
  "description": "Microservicio de identidad y sesión (JWT) — esqueleto alineado con dotbin-server",
  "main": "src/server/server.js",
  "type": "commonjs",
  "private": true,
  "scripts": {
    "start": "node src/server/server.js",
    "dev": "nodemon src/server/server.js",
    "test": "node --test",
    "prisma:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^5.19.1",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^5.1.0",
    "joi": "^17.13.3",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "prisma": "^5.19.1"
  }
}
